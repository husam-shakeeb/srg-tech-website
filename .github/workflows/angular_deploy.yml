name: Build and Deploy
on:
  push:
    branches:
      - some-branch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
       token: ${{ secrets.ACCESS_TOKEN }}
    - run: git config --global user.email "husam_shakeeb@hotmail.com"   
    - run: git config --global user.name "husam-shakeeb"
    - run: npm ci
    - run: npx ng deploy --base-href=/srg-tech-website/ --no-silent